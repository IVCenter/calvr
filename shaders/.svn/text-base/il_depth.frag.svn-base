uniform sampler2D MyFixPipeTex;

void main()
{
  //gl_FragColor = vec4(gl_Color.r, gl_Color.g, gl_Color.b, gl_Color.a);
  //gl_FragColor = gl_Color;
  gl_FragColor = texture2D(MyFixPipeTex,gl_TexCoord[0].st);
}
